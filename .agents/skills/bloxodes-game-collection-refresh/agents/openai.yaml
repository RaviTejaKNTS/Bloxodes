interface:
  display_name: "Bloxodes Game Collection Refresh"
  short_description: "Check existing collections for new data and images"
  default_prompt: "Use $bloxodes-game-collection-refresh to quickly update existing collection datasets with verified new data and missing item images, and stop when nothing changed."
