interface:
  display_name: "Bloxodes Catalog Writing"
  short_description: "Write global catalog pages"
  default_prompt: "Use $bloxodes-catalog-writing to write one global catalog final.json from approved brief.md. Explain the collection clearly, avoid website-first wording, and parse-check final.json."
