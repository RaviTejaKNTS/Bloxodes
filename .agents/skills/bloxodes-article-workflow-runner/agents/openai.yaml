interface:
  display_name: "Bloxodes Article Workflow Runner"
  short_description: "Run article research, images, writing, and QA"
  default_prompt: "Use $bloxodes-article-workflow-runner to run approved article ideas with separate research, mandatory image, and writing subagents. Require media.json for every article, verify each final, and preview it in an available real browser such as Chrome."
