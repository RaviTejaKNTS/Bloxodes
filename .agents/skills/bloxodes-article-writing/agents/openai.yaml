interface:
  display_name: "Bloxodes Article Writing"
  short_description: "Write Bloxodes articles"
  default_prompt: "Use $bloxodes-article-writing to write one Bloxodes article final.json from an approved brief.md. Include game name in title/slug for game articles, add useful faq_json only for follow-up questions, avoid self-referential wording, then parse-check final.json."
