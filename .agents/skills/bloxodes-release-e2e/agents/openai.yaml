interface:
  display_name: "Bloxodes Release E2E"
  short_description: "Release a worktree safely through production"
  default_prompt: "Use $bloxodes-release-e2e to release this completed worktree through the smallest safe direct-or-PR path, production verification, local sync, and cleanup."
