interface:
  display_name: "Bloxodes Release E2E"
  short_description: "Quickly publish completed work to production"
  default_prompt: "Use $bloxodes-release-e2e to publish this approved worktree directly to production, verify the required live result, sync local production, and keep this worktree open for immediate follow-up."
