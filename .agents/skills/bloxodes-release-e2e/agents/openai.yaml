interface:
  display_name: "Bloxodes Release E2E"
  short_description: "Publish and synchronize Bloxodes production"
  default_prompt: "Use $bloxodes-release-e2e to publish this approved worktree, verify production, synchronize local production and the homelab checkout to the exact SHA, and keep the task worktree open."
