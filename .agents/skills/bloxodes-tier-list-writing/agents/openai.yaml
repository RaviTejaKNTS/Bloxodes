interface:
  display_name: "Bloxodes Tier List Writing"
  short_description: "Write image-backed Bloxodes tier-list articles"
  default_prompt: "Use $bloxodes-tier-list-writing to write one source-backed Bloxodes tier-list article from an approved brief and mandatory media.json, including the visual overview block and matching per-tier detail tables when exact images are verified, with text fallback only for accepted-missing targets."
