interface:
  display_name: "Bloxodes Tool Writing"
  short_description: "Write tool page content"
  default_prompt: "Use $bloxodes-tool-writing to write research-notes.md and final.json for one tool page. Read agents/content-writing/agents.md, verify the input/output job and formula, then write simple tool copy."
